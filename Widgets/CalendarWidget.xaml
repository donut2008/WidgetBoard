<UserControl
    x:Class="WidgetBoard.Widgets.CalendarWidget"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WidgetBoard.Widgets"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
	<Grid>
		<StackPanel Orientation="Vertical" Spacing="5" Margin="8">
			<StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
				<TextBlock Style="{StaticResource SubtitleTextBlockStyle}">Calendar</TextBlock>
                <Button HorizontalAlignment="Right" Margin="8">
                    <Button.Content>
						<FontIcon Glyph="&#xE10C;"/>
					</Button.Content>
					<Button.Flyout>
						<MenuFlyout>
							<MenuFlyoutItem Text="Hide widget">
								<MenuFlyoutItem.Icon>
									<FontIcon Glyph="&#xE10A;"/>
								</MenuFlyoutItem.Icon>
							</MenuFlyoutItem>
						</MenuFlyout>
					</Button.Flyout>
				</Button>
			</StackPanel>
		</StackPanel>
	</Grid>
</UserControl>
